package accessMod.operations;

public class AdvaCalc {
    public double power(int base, int exp)
    {
        return Math.pow(base, exp);
    }

    public double absolVal(int num)
    {
        return Math.abs(num);
    }
}
